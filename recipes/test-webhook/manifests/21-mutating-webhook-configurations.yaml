# The mutating half of this recipe's admission surface: two
# `MutatingWebhookConfiguration`s, not one (Task 3.9 Step 4). Both call
# the same server, on different paths, and each performs a disjoint set
# of `test.admissionlab.io/*` actions
# (`crates/admissionlab-test-webhook/src/mutate.rs`'s `MutationScope`):
#
#   - `...-mutate-containers` -> `/mutate-containers`: `add-container`,
#     `add-init-container`, `remove-container`, `remove-init-container`,
#     `add-volume`.
#   - `...-mutate-labels`     -> `/mutate-labels`:     `add-label`.
#
# # Why two, and how the reinvocation chain works
#
# Kubernetes reinvokes a mutating webhook only when something changed the
# object *after* that webhook already ran, so a single webhook can never
# exercise `reinvocationPolicy` at all. Two are the minimum, and they
# have to be able to affect each other:
#
#   - `...-mutate-containers` sorts first (configurations are evaluated
#     in lexicographic name order) and mutates `spec`.
#   - `...-mutate-labels` runs after it and mutates `metadata.labels`.
#
# So the dedicated reinvocation fixture
# (`fixtures/core/admission/pod-reinvocation.yaml`) is a pod that carries
# the gate label in its own manifest *and* asks both webhooks to act: the
# containers webhook appends a volume, the labels webhook then adds a
# label, and because the object changed after the containers webhook ran,
# the API server calls the containers webhook a second time. Its action
# is idempotent, so the round-1 evidence is an invocation record with no
# patch — which is exactly the observable this project needs.
#
# ## What the `objectSelector` cannot do (corrected in Task 3.10)
#
# Task 3.9 wrote this section claiming the opposite chain: a pod with no
# gate label, the labels webhook adding it, and the containers webhook
# then matching on reinvocation. That is not how kube-apiserver works,
# and its own mutating dispatcher says so in as many words
# (`staging/src/k8s.io/apiserver/pkg/admission/plugin/webhook/mutating/dispatcher.go`,
# read at `release-1.36`):
#
#   // This means that during reinvocation, a webhook will not be
#   // called for the first time. For example, if the webhook is
#   // skipped in the first round because of mismatching labels,
#   // even if the labels become matching, the webhook does not
#   // get called during reinvocation.
#
# Only webhooks that were already invoked before the mutation are
# reinvoked — `reinvocationcontext.go`'s
# `RequireReinvokingPreviouslyInvokedPlugins` moves the
# previously-invoked set into the reinvoke set, and
# `ShouldReinvokeWebhook` consults only that set. Confirmed live against
# `kind` v1.36.4: the label-gated fixture produced one round-0 invocation
# of the labels webhook, no round-1 annotation at all, and an admitted
# object the containers webhook never touched. Nothing about *these
# manifests* changed as a result — the `objectSelector` still does its
# real job of keeping the two webhooks' fixtures independent — only this
# description of what it can be used for.
#
# # Correctness does not depend on the invocation order
#
# The roadmap is explicit that it must not, and this design satisfies
# that in two independent ways.
#
# First, `reinvocationPolicy: IfNeeded` is set on *both* webhooks, not
# only on the one the chain happens to need. Whichever the API server
# calls first, the other's output can still reach it: if the ordering
# ever reversed, the labels webhook would run first and the *labels*
# webhook would be the one reinvoked, with the same final object either
# way.
#
# Second, every action the server performs is idempotent: an add whose
# target is already present emits no patch at all
# (`crates/admissionlab-test-webhook/src/mutate.rs`, and
# `tests/behavior.rs` asserts it). Reinvocation therefore converges
# instead of appending duplicate containers, and "how many times was
# this webhook called" is never something a fixture's outcome depends
# on.
#
# # Scoping, `failurePolicy`, `sideEffects`, `timeoutSeconds`
#
# Identical in shape and reasoning to
# `20-webhook-configuration.yaml` — read that file's comments, which
# cover the namespace opt-in (and why it structurally excludes
# `kube-system` and this recipe's own namespace), `sideEffects: None`
# and dry-run visibility, `matchPolicy: Equivalent`, and why
# `failurePolicy: Fail` is the honest choice for a component that exists
# to be observed. The one difference is `timeoutSeconds: 5` rather than
# 10: the mutating routes never sleep — `test.admissionlab.io/delay-ms`
# is the validating webhook's alone, deliberately, so a fixture's delay
# is applied exactly once per request regardless of how many mutating
# configurations are installed (see
# `crates/admissionlab-test-webhook/src/validate.rs`) — so 5 seconds
# here is pure scheduling and network headroom, not a budget a fixture
# can spend.
#
# `caBundle` is omitted in both, and filled in at runtime by the
# `bootstrap` init container, exactly as for the validating
# configuration. Both names below must appear in that container's
# `ADMISSIONLAB_TEST_WEBHOOK_MUTATING_WEBHOOK_CONFIGURATION_NAMES`
# environment variable (`30-deployment.yaml`) and in the `resourceNames`
# of the ClusterRole that lets it write them (`10-rbac.yaml`).
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  # Sorts before `...-mutate-labels`, so this is the webhook that runs
  # first and therefore the one the reinvocation fixture observes being
  # called again — see this file's own comments on why nothing about the
  # *result* depends on that ordering.
  name: admissionlab-test-webhook-mutate-containers
  labels:
    app.kubernetes.io/name: admissionlab-test-webhook
webhooks:
  - name: mutate-containers.test-webhook.admissionlab.dev
    admissionReviewVersions: ["v1"]
    sideEffects: None
    matchPolicy: Equivalent
    failurePolicy: Fail
    timeoutSeconds: 5
    reinvocationPolicy: IfNeeded
    namespaceSelector:
      matchLabels:
        admissionlab.dev/test-webhook: "enabled"
    # Scopes this webhook to the fixtures that opt into it: a pod
    # without this label is never sent to `/mutate-containers` at all,
    # so a fixture exercising the labels webhook alone is never also
    # mutated by this one. Note that a label added *during* admission
    # cannot bring a pod in here — see this file's own comments on what
    # the `objectSelector` cannot do.
    objectSelector:
      matchLabels:
        test.admissionlab.io/containers: "enabled"
    rules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
        scope: "Namespaced"
    clientConfig:
      service:
        name: admissionlab-test-webhook
        namespace: admissionlab-test-webhook
        # Must equal
        # `admissionlab_test_webhook::serve::MUTATE_CONTAINERS_PATH`;
        # `crates/admissionlab-test-webhook/tests/behavior.rs` asserts it.
        path: /mutate-containers
        port: 443
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: admissionlab-test-webhook-mutate-labels
  labels:
    app.kubernetes.io/name: admissionlab-test-webhook
webhooks:
  - name: mutate-labels.test-webhook.admissionlab.dev
    admissionReviewVersions: ["v1"]
    sideEffects: None
    matchPolicy: Equivalent
    failurePolicy: Fail
    timeoutSeconds: 5
    # `IfNeeded` here too, even though the chain as written only needs
    # it on the containers webhook — see this file's own comments on
    # order independence. It costs nothing: this webhook's own action is
    # idempotent, so a reinvocation of it emits no patch.
    reinvocationPolicy: IfNeeded
    namespaceSelector:
      matchLabels:
        admissionlab.dev/test-webhook: "enabled"
    # Deliberately no `objectSelector`: this is the webhook that has to
    # be able to run on a pod that does not yet carry the gate label, so
    # gating it on any label of its own would break the chain it exists
    # to start.
    rules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
        scope: "Namespaced"
    clientConfig:
      service:
        name: admissionlab-test-webhook
        namespace: admissionlab-test-webhook
        # Must equal
        # `admissionlab_test_webhook::serve::MUTATE_LABELS_PATH`;
        # `crates/admissionlab-test-webhook/tests/behavior.rs` asserts it.
        path: /mutate-labels
        port: 443
